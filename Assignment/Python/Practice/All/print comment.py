def add(a: int, b: int) -> int:
    """
    Adds two integers.

    Args:
        a (int): The first integer.
        b (int): The second integer.

    Returns:
        int: The sum of a and b.
    """

    """Hello"""

    "In Python, an unassigned string literal like 'Hello' is simply ignored by the interpreter, unless it's part of a docstring (which is typically the first string in a function or class)."

    # "Single or double quotes (' or ") can be used for one-line docstrings, but it's less common.
    # Triple quotes (""" or ''') are the preferred and recommended way to write docstrings, especially for multi-line documentation."

    return a + b

print(add.__doc__)

